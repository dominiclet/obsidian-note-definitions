import { App, TFile } from "obsidian";
import { ConsolidatedDefParser } from "src/core/consolidated-def-parser";

test('Test consolidated definition parser', async () => {
	new ConsolidatedDefParser(null, null)
});
